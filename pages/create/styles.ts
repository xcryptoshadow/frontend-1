import { SxProps } from "@mui/material";

interface IStyles {
  backIcon: SxProps;
}

export const styles: IStyles = {
  backIcon: {
    position: "relative",
    left: "4px",
    color: "primary.buttonColor",
  },
};
